name: Quality Checks

on:
  push:
    branches: main
  pull_request:
    branches: main
  workflow_dispatch:

jobs:
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check file structure
        run: |
          echo "📁 Verificando estrutura do projeto..."
          echo "✅ README.md existe: $([ -f README.md ] && echo 'SIM' || echo 'NÃO')"
          echo "✅ Diretório test/ existe: $([ -d test/ ] && echo 'SIM' || echo 'NÃO')"
          echo "✅ login.interation.test.js existe: $([ -f test/login.interation.test.js ] && echo 'SIM' || echo 'NÃO')"
          echo "✅ login.virtual.users.test.js existe: $([ -f test/login.virtual.users.test.js ] && echo 'SIM' || echo 'NÃO')"
          echo "✅ package.json existe: $([ -f package.json ] && echo 'SIM' || echo 'NÃO')"
          echo "✅ package-lock.json existe: $([ -f package-lock.json ] && echo 'SIM' || echo 'NÃO')"

      - name: Validate test files syntax
        run: |
          echo "🔍 Validando sintaxe dos arquivos de teste..."
          if node -c test/login.interation.test.js; then
            echo "✅ login.interation.test.js - OK"
          else
            echo "❌ login.interation.test.js - ERRO DE SINTAXE"
            exit 1
          fi

          if node -c test/login.virtual.users.test.js; then
            echo "✅ login.virtual.users.test.js - OK"
          else
            echo "❌ login.virtual.users.test.js - ERRO DE SINTAXE"
            exit 1
          fi

      - name: Lint JavaScript files
        run: |
          echo "🔍 Executando ESLint..."
          if npx eslint test/*.js --format=compact; then
            echo "✅ ESLint passou sem erros"
          else
            echo "❌ ESLint encontrou problemas"
            exit 1
          fi

      - name: Quality Check Summary
        run: |
          echo "🎉 Todos os checks de qualidade passaram!"
          echo "✅ Estrutura do projeto: OK"
          echo "✅ Sintaxe JavaScript: OK"
          echo "✅ Padrões de código: OK"
